"use strict";

var React = require('react')
  , Immutable = require('immutable')

module.exports = React.createClass({
  displayName: 'Panes',

  propTypes: {
  },

  render() {
    return (
      <div>
      </div>
    )
  }
});
